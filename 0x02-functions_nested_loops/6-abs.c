#include "main.h"

/**
 * _abs - prints the absolute value of a number
 * @int: the value to be computed
 * Return: absolute value
 */
int _abs(int)

{
	if (int >= 0)
		return (int);
	else
		return (-int);
}


#include "main.h"

/**
 * _abs - prints the absolute value of a number
 * @t: the value to be computed
 * Return: absolute value
 */
int _abs(int t)

{
	if t >= 0)
		return (t);
	else
		return (-t);
}

